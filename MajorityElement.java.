public class MajorityElement {
    public int majorityElement(int[] nums) {
        int candidate = nums[0]; // Изначально выбираем первый элемент как кандидата
        int count = 1; // У нас уже есть один экземпляр кандидата
        
        // Проходим по всем элементам массива
        for (int i = 1; i < nums.length; i++) {
            if (count == 0) {
                // Если счетчик равен 0, меняем кандидата
                candidate = nums[i];
                cou
